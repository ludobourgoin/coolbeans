---
// L'ancienne page « Mon site » (S0.7) a éclaté en trois pages avec la sidebar
// (spec 2026-08-14) : Monitoring, SEO, Analytics. On redirige vers la première
// pour ne pas casser les liens existants.
export const prerender = false;

import { portalHref } from "../../lib/portail/nav";

return Astro.redirect(portalHref("/monitoring", Astro.url.hostname), 301);
---
