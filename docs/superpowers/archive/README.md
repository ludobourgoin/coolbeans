# Archive — specs et plans terminés

> **Ne jamais référencer ces fichiers dans du travail courant.** Tout ce qui est ici est
> soit livré (le durable a migré vers la documentation ou le code), soit caduc (ère Asana).
> Ils ne sont conservés que pour l'archéologie : comprendre pourquoi une décision a été
> prise, retrouver un raisonnement.

La source de vérité des specs vivantes est `../specs/README.md`.

| Fichier | Sort |
| --- | --- |
| `*astro-tailwind*`, `*design-system-geist*`, `*deploy-workers*` | Livrés (site, design system, migration Workers) |
| `*pilotage-tarifaire*`, `*selecteur-de-client-admin*` | Livrés (chiffrages, vue admin) |
| `*sop-commercial*` | Livré (SOP dans la doc Coolbeans + `src/data/sop.ts`) |
| `*migration-asana-linear*`, `*crm-linear*` | Exécutés (migrations des 2026-08-13 et 2026-08-16) |
| `*doc-client*` | Livré (skill `doc-client`) |
| `brief-portail-client-asana.md`, `*portail-cpu-scheduled*`, `*portail-sync-par-team*` | Caducs : sync Asana retiré (COO-22) |
| `*portail-publicmetadata*` | Remplacé : `src/lib/portail/metadata.ts` fait foi |
